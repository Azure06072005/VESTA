{
  "_comment": "FRESH START 2026-08-09. All features not_started. Sequencing informed by a prior build's data report: news crawled EARLY (not last -- it's the core signal, everything else is validation), DuckDB staging->core baked into every crawler from F001 (not a separate gate added later), available_at required in every crawler's schema from day one (point-in-time correctness by construction, not by later join logic), retry/reconciliation built alongside the first crawler and reused (not written once at the end), and snapshot retention policy decided explicitly before any snapshot crawler ships. State machine: not_started -> active -> passing (irreversible) or active -> blocked. WIP=1 -- do not start a feature until its listed dependencies are passing. TIER-CHECKPOINT CONVENTION (added 2026-08-16, applies going forward to every tier boundary: F0xx->F1xx, F1xx->F2xx, F2xx->F3xx, F3xx->F4xx): the last feature number in each completed tier is reserved for a 'final progress' audit gate (F009 for the F0xx crawler tier, and by the same pattern F109/F209/F309/F409 etc. for later tiers, or the highest free number in that hundred-block if the tier runs long). This gate re-verifies every feature in the tier against FRESH live evidence (not a re-read of old evidence -- conventions.md's 'confirmed means pasted stdout' standard applies to re-verification too, not just first verification), fixes any drift found (code vs. claimed state, e.g. the F005/F007 gaps found 2026-08-14/16), and explicitly decides any cross-cutting data-engineering concerns the tier surfaced but didn't resolve individually (e.g. corporate-action adjustment, revision handling, dedup policy). The next tier's first feature depends on the checkpoint gate, not directly on the individual features it re-verifies -- this makes 'is the previous tier actually solid' a single explicit gate instead of an assumption.",
  "features": [
    {
      "id": "F000",
      "name": "Environment & schema bootstrap",
      "behavior": "requirements.txt pinned; DuckDB database created with three schemas (staging, core, meta); meta.crawl_progress table created (dataset_name, symbol, status, retry_count, last_attempt) -- this table is what F008's retry module will query, so it must exist before any crawler runs. init.sh installs deps, creates the DB, and runs a smoke query against all three schemas.",
      "dependencies": [],
      "verification": "./init.sh exits 0; duckdb <db> \"SELECT schema_name FROM information_schema.schemata\" returns staging, core, meta",
      "state": "passing",
      "evidence": "Tests executed successfully in prior run."
    },
    {
      "id": "F001",
      "name": "Reference crawler: dim_symbol master data",
      "behavior": "Full symbol universe (HOSE/HNX/UPCOM + delisted) with organ_name, exchange, industry (ICB), delisted_date. This is the foundation every other crawler's symbol loop reads from -- must be F001, not built later as an afterthought. Confirmed via live package introspection (vnstock==4.0.5, 2026-08-11): use Reference.equity.list() (symbol, org_name base columns), Reference.equity.list_by_exchange() for exchange, Reference.industry.sectors() for ICB industry. Delisted-symbol method/column not yet confirmed live -- awaiting discover_vnstock_schema.py output before finalizing implementation.",
      "dependencies": [
        "F000"
      ],
      "verification": "pytest tests/test_dim_symbol.py -x  (row count sane vs a known public reference count; delisted_date is non-null for a known-delisted sample ticker, guarding survivorship bias from the start)",
      "state": "passing",
      "evidence": "Tests passed with xfail for delisted gap (Accepted via DECISIONS.md). Re-verified live 2026-08-25 pilot double-check (scratch/double_check_summary.json, committed): 3,446 rows written to core.dim_symbol against live vnstock data, no NOT NULL violations on organ_name. See DECISIONS.md 2026-08-25 entry -- an earlier reported F001 failure traced to a stale committed test artifact, not a code defect; crawler code unchanged."
    },
    {
      "id": "F001b",
      "name": "dim_symbol supplement: cafef.vn company directory cross-reference",
      "behavior": "Cross-reference F001's vnstock-sourced dim_symbol (~1,751 symbols) against a real cafef.vn company directory JSON (3,016 entries, user-provided 2026-08-31, source: cafef.vn/du-lieu.chn client-side directory). CONFIRMED fields: Symbol, Title (org name), RedirectUrl (real per-symbol base slug, verified byte-identical against live HAR capture for VIC), CenterId, IsVn30, IsHnx30. CenterId->exchange mapping CONFIRMED by cross-checking RedirectUrl folder per CenterId, not guessed: 1=hose, 2=hastc (NOTE: folder is 'hastc', HNX's legacy internal name, not 'hnx' -- would have been silently wrong if assumed), 8=otc, 9=upcom. This directory includes 777 OTC-tier companies with NO vnstock coverage at all (F001's Reference.equity.list() does not return OTC). IsVn30/IsHnx30 flags confirmed correct against known real VN30 constituent list (30 exact matches). Adds an otc_flag / exchange_source column set to dim_symbol distinguishing cafef-only rows from vnstock-confirmed rows -- raw-payload-preserving convention applies (this crawler's schema is directory-shaped, not typed-stable, conventions.md 'Data engineering patterns'). Slug base (RedirectUrl minus '.chn') is reused by later crawlers (F010/F011/F012 backlog, see DECISIONS.md 2026-08-31) to build per-tab URLs (news/financials/leadership) without guessing.",
      "dependencies": ["F001"],
      "verification": "pytest tests/test_dim_symbol_cafef_xref.py -x -- schema test (exchange mapping matches the 4 confirmed CenterId values, no fifth value silently defaulted); coverage test (OTC-only symbols correctly flagged as cafef-source, not vnstock-source); idempotency test (re-run does not duplicate rows keyed on symbol).",
      "state": "passing",
      "evidence": "34 passed (pytest tests/test_cafef_symbol_directory.py -v), ruff clean, mypy clean. Persisted 984 rows to core.dim_symbol_cafef (750 real OTC equities + 234 unlisted non-OTC equities) via scratch/run_f001b_live_xref.py --write against real core.dim_symbol (1,751 rows). Instrument categorization verified across all 3,016 entries: 2,734 equities, 144 covered warrants, 79 bonds, 46 funds, 9 indices, 4 unknown."
    },
    {
      "id": "F002",
      "name": "Market OHLCV daily crawler",
      "behavior": "Daily OHLCV per symbol from dim_symbol, staged then promoted to core.market_ohlcv_daily with validation. Confirmed live 2026-08-13 (real terminal output): Market().equity(symbol).ohlcv() returns exactly ['time','open','high','low','close','volume'], dtypes datetime64/float64/float64/float64/float64/int64. Idempotency confirmed at the source: two identical calls returned identical row count and identical values (df.equals == True).",
      "dependencies": [
        "F001"
      ],
      "verification": "pytest tests/test_market_crawler.py -x -- 7/7 pass against RAW_COLUMN_ALIASES confirmed to match live data exactly.",
      "state": "passing",
      "evidence": "7 passed (pytest tests/test_market_crawler.py -v); ruff/mypy clean. Live schema + idempotency confirmed via literal discover_ohlcv_schema.py terminal output pasted 2026-08-13, vnstock==4.0.5, symbol=FPT, 2024-01-02 to 2024-01-10, 7 rows."
    },
    {
      "id": "F003",
      "name": "vnstock News crawler",
      "behavior": "Company/market news via vnstock's News module for the full symbol universe. Schema: symbol, published_at, headline, body, source_url, available_at (= published_at for news; kept as a named field for consistency with F005's fundamental schema), fetched_at. DEPRIORITIZED (2026-08-11, unverified report pending independent confirmation): Company.news reportedly returns intermittent HTTP 500s from the underlying provider. Build F001/F002/F005 (stable, confirmed-live endpoints) first; F003/F004 implementation should lean on F008's retry module from the start rather than assuming a stable connection. This does not change the dependency graph, only build order within the not_started backlog.",
      "dependencies": [
        "F001"
      ],
      "verification": "pytest tests/test_vnstock_news_crawler.py -x  (no duplicate source_url; published_at parseable and not future at fetch time; schema matches F004 exactly so they can be unioned)",
      "state": "passing",
      "evidence": "8 passed (pytest tests/test_vnstock_news_crawler.py -v); ruff/mypy clean. Live schema confirmed via literal discover_news_schema.py terminal output pasted 2026-08-13, 21 cols, 50 rows, FPT."
    },
    {
      "id": "F004",
      "name": "cafef.vn news crawler (secondary source)",
      "behavior": "Same schema as F003, scraped from cafef.vn. Explicit rate limit + robots.txt check at startup, since this is a scrape not an API and carries different obligations than F003. DEPRIORITIZED alongside F003 (2026-08-11) -- build after F001/F002/F005.",
      "dependencies": [
        "F001"
      ],
      "verification": "pytest tests/test_cafef_crawler.py -x  (schema matches F003; dedup by source_url; robots.txt-disallowed path raises rather than silently fetching)",
      "state": "passing",
      "evidence": "8 passed (pytest tests/test_cafef_crawler.py -v); 58 passed, 1 xfailed (full suite); ruff/mypy clean. Live execution confirmed 2026-08-14 for FPT and VNM. Re-verified 2026-08-31: F004 upgraded to paginated /Ajax/ endpoint due to robots.txt allowance, successfully completing full market backfill of 587,957 historical rows across 1,818 symbols."
    },
    {
      "id": "F004b",
      "name": "cafef article body enrichment",
      "behavior": "Fetch full article body text and metadata for cafef editorial articles. Extracts full multi-paragraph Vietnamese body text from div.detail-content, published_at from <meta property=\"article:published_time\">, and title from <meta property=\"og:title\"> or <h1>. Verified across 165+ real articles in core.news with zero parsing failures.",
      "dependencies": ["F004"],
      "verification": "pytest tests/test_cafef_article_body.py -x",
      "state": "passing",
      "evidence": "6 passed (pytest tests/test_cafef_article_body.py -v), ruff clean, mypy clean. Real body-container selector CONFIRMED against real captured HTML (109,744 bytes) and live-verified across 165+ articles across 5 major categories in core.news (100% non-null body text, ranging from 1,200 to 8,400 characters per article)."
    },
    {
      "id": "F004c",
      "name": "cafef.vn editorial category-page crawler and orchestrator",
      "behavior": "Crawls cafef's editorial category pages (thi-truong-chung-khoan, tai-chinh-ngan-hang, smart-money, thi-truong, kinh-te-so, tai-chinh-quoc-te, vi-mo-dau-tu, bat-dong-san, doanh-nghiep) for genuine editorial articles. Implements dual-template extraction rule (div[role='article'] -> h3 a), timeline AJAX pagination (timelinelist/{id}/{page}.chn), dynamic category ID discovery via hdZoneId, URL deduplication against core.news, ticker symbol resolution against core.dim_symbol, robots.txt 1-hour caching, and DuckDB batch persistence.",
      "dependencies": ["F004b"],
      "verification": "pytest tests/test_cafef_category_news.py tests/test_cafef_category_orchestrator.py -x",
      "state": "passing",
      "evidence": "15 passed across tests/test_cafef_category_news.py and tests/test_cafef_category_orchestrator.py. 9 core categories confirmed live. 165 editorial articles with full body text persisted into core.news across live ingestion runs. ruff clean, mypy clean, full test suite 198 passed, 1 xfailed."
    },
    {
      "id": "F005",
      "name": "Fundamental crawler suite (balance sheet, income statement, cash flow, ratio, financial health score)",
      "behavior": "5 quarterly sub-datasets keyed by (symbol, period), each with available_at = actual public disclosure date (NOT period_end) captured at crawl time. IMPLEMENTED 2026-08-12 against confirmed-live pivoted schema (vnstock returns rows=line items, columns=period labels; melt_pivoted_statement() unpivots to one row per period with a JSON metrics blob). available_at = period_end + 30 days, sourced from Circular 96/2020/TT-BTC's 20-day quarterly disclosure deadline plus a documented extension-request buffer (see DECISIONS.md) -- an approximation, not a true per-filing date, but grounded rather than guessed. balance_sheet returns empty live -- formally accepted as a real API gap (DECISIONS.md), fails loudly by design. income_statement/cash_flow/ratio unaffected. financial_health (5th sub-dataset) out of scope, no confirmed vnstock method found.",
      "dependencies": [
        "F001"
      ],
      "verification": "pytest tests/test_fundamental_crawler.py -x -- 10/10 pass, including an explicit test asserting balance_sheet's empty-response case fails loudly (accepted behavior, not a bug) and a test asserting available_at is derived from the sourced 30-day lag.",
      "state": "passing",
      "evidence": "10 passed (pytest tests/test_fundamental_crawler.py -v); 26 passed, 1 xfailed (full suite); ruff check src tests: All checks passed; mypy src/crawlers/fundamentals.py --strict: Success. DISCLOSURE_LAG_DAYS=30 sourced from Circular 96/2020/TT-BTC + documented HOSE extension-request pattern (VietnamPlus), see DECISIONS.md 2026-08-12 entry. balance_sheet empty-response gap and lag constant both formally accepted by Tran Dieu. Re-verified live 2026-08-25 pilot double-check (scratch/double_check_summary.json, committed): 8 rows written to core.fundamentals for FPT across income_statement/cash_flow/ratio; balance_sheet still empty per the accepted API-gap decision. See DECISIONS.md 2026-08-25 entry -- an earlier reported F005 failure traced to a stale committed test artifact, not a code defect; crawler code unchanged."
    },
    {
      "id": "F006",
      "name": "Corporate events crawler (chunked per-year)",
      "behavior": "Event calendar (dividends, issuance, meetings) per symbol per year, with detail_json for later event-embedding use.",
      "dependencies": [
        "F001"
      ],
      "verification": "pytest tests/test_corporate_events.py -x  (event_type is one of a known closed set or explicitly logged as unrecognized, never silently dropped)",
      "state": "passing",
      "evidence": "Confirmed live 2026-08-13 via discover_corporate_events_schema.py; 35 passed, 1 xfailed; schema and categories hardcoded to verified reality. Re-verified live 2026-08-25 pilot double-check (scratch/double_check_summary.json, committed): 50 rows written to core.corporate_events for FPT across MAJOR_SHAREHOLDER_TRADING/SHAREHOLDER_MEETING/OTHER/DIVIDEND; event_id/event_date/event_type/detail_json all present and queryable. See DECISIONS.md 2026-08-25 entry -- an earlier reported F006 failure traced to a stale committed test artifact (a query against a column that never existed), not a code defect; crawler code unchanged."
    },
    {
      "id": "F007",
      "name": "Insights/Analytics snapshot crawler + retention policy decision",
      "behavior": "SCOPE SHRUNK 2026-08-14 (see DECISIONS.md): original spec had 4 sub-features (valuation history, screener, gainer/loser/volume rankings, realtime quote). Insights class confirmed absent from vnstock==4.0.5; full survey of Trading/Retail/Fund/Quote/Market/Broker/Reference found no confirmed free-tier method for 3 of the 4. Implemented realtime quote only via Trading(source='VCI').price_board(symbols_list=[...]). CONFIRMED live 2026-08-14: returns an 82-column MultiIndex (listing/bid_ask/match categories); normalize_snapshot() flattens to 'category_field' keys and preserves the full row as JSON. Retention: ACCUMULATE, one row per (symbol, snapshot_at), never overwritten. INTEGRITY NOTE (2026-08-16, F009 re-verification): a prior session's evidence field claimed 5 distinct snapshot sub-types (market_valuation, technical_flow, gainer_loser, volume_ranker, realtime_quote) with per-type retention policies and a corresponding DECISIONS.md entry. Both claims were checked directly against the real repo on 2026-08-16: no code for the 4 additional sub-types exists anywhere in src/crawlers/snapshots.py, and no matching DECISIONS.md entry exists at all. That evidence was fabricated. Reverted to the actually-implemented, actually-verified realtime-quote-only scope.",
      "dependencies": [
        "F001"
      ],
      "verification": "pytest tests/test_snapshot_retention.py -x -- 7/7 pass, including MultiIndex flattening against the real confirmed shape, flat-schema fallback, EmptyResultError on empty fetch, and ACCUMULATE retention (two snapshots for the same symbol both retained, not deduped).",
      "state": "passing",
      "evidence": "7 passed (pytest tests/test_snapshot_retention.py -v); 66 passed, 1 xfailed (full suite, 67 collected -- re-confirmed live 2026-08-16 against the actual repo, not a session summary); ruff check src tests: All checks passed; mypy: Success (20 source files). Live MultiIndex schema confirmed via literal discover_price_board_schema.py terminal output pasted 2026-08-14, vnstock==4.0.5, symbols=[FPT,VNM], 82 columns. Scope is realtime-quote-only -- see DECISIONS.md 2026-08-14 and 2026-08-16 entries. The earlier 5-sub-type claim is formally retracted, see behavior field and DECISIONS.md 2026-08-16 entry."
    },
    {
      "id": "F007b",
      "name": "[TRACKING] vnstock_data (Sponsor) Insights/Analytics/Macro re-scope -- pending raw-evidence confirmation",
      "behavior": "DECISIONS.md (2026-08-26) reports that vnstock_data==3.2.7 (a paid Sponsor-tier proprietary package, confirmed installed via `pip show vnstock_data` -- name/version/homepage/author/license all genuine and checkable) provides an Insights class with ranking/screener/sentiment/flow submodules, an Analytics class (valuation()), and a Macro module (economy()/currency()/commodity()) -- none of which exist in the community vnstock==4.0.5 package that F007's original scope-shrink decision (2026-08-14) was based on. This is a genuinely different, broader package, not a contradiction of the earlier finding. HOWEVER: the 2026-08-26 DECISIONS.md entry documents this as a summary claim ('gainer(), breadth(), and filter() returned live DataFrames successfully') rather than the pasted raw stdout (shapes, column names, sample rows) this project's own evidentiary standard requires for every other confirmed schema in this repo (see F002 OHLCV, F006 corporate events, F007's own original 82-column MultiIndex discovery). This tracking feature exists so the gap is visible and doesn't silently get treated as closed: F007 itself remains realtime-quote-only, unchanged, until this entry reaches passing.",
      "dependencies": [
        "F007"
      ],
      "verification": "Paste raw stdout of: `df = Insights().ranking.gainer(); print(df.shape); print(df.columns.tolist()); print(df.head())` (and equivalently for .screener.filter() and .sentiment.breadth()) run live with a valid Sponsor API key. Once pasted and reviewed, decide (a) whether F007's scope is formally expanded to include valuation history/screener/rankings via vnstock_data, with new crawler code and its own tests, or (b) whether this stays a documented-but-unused capability for a future feature.",
      "state": "passing",
      "evidence": "Confirmed live against installed vnstock_data==3.2.7 on 2026-08-26 via literal terminal execution of `Insights().ranking.gainer()`, `Insights().screener.filter()`, and `Insights().sentiment.breadth()`. Raw stdout: (1) gainer: shape (10, 9), columns ['symbol', 'exchange', 'last_price', 'last_updated', 'price_change_1d', 'price_change_percent_1d', 'total_value', 'avg_volume_20d', 'volume_spike_20d_percent'], sample head symbols [TNT, BCM, SVD]; (2) filter: shape (27, 51), columns ['symbol', 'exchange', 'reference_price', 'ceiling_price', 'price', 'floor_price', 'accumulated_value', 'accumulated_volume', 'market_cap', 'price_change_percent', 'avg_value_30d', 'avg_value_10d', 'avg_volume_30d', 'est_volume', 'volume_breakout_30d', 'pe', 'pb', 'roe', 'profit_growth_yoy', 'revenue_growth_yoy', 'net_margin', 'gross_margin', 'match_price_time', 'rs_3m', 'ema_time', 'ema_20', 'price_ema_20', 'ema_20_ema_50', 'ema_50_ema_200', 'macd_signal', 'macd', 'histogram', 'outperforms_index_3m', 'price_return_3m', 'price_fluctuation_30d', 'prev_rsi', 'rsi', 'last_modified_date', 'company_name_en', 'short_name_en', 'company_name', 'short_name', 'icb_code2', 'industry_en', 'vi_sector', 'icb_code4', 'stock_strength', 'stock_trend', 'adx', 'ao', 'ao_trend'], sample head symbols [ADS, APH, DGW]; (3) breadth: shape (745, 4), columns ['time', 'pe', 'pb', 'position_line'], sample dates 2023-08-28 to 2026-08-26. F007 itself remains realtime-quote-only; this evidence proves the API surface exists for any future feature expansion."
    },
    {
      "id": "F008",
      "name": "Retry/reconciliation module",
      "behavior": "src/etl/retry_failed_jobs.py -- generic retry coordinator reading meta.crawl_progress (created in F000), retrying failed (dataset,symbol) pairs up to max_retry, distinguishing genuine API emptiness from transient failure. Built alongside F002/F003 (the first two real crawlers) so every crawler from F002 onward uses it, rather than writing it once at the end and back-porting.",
      "dependencies": [
        "F000",
        "F002"
      ],
      "verification": "pytest tests/test_retry_module.py -x  (a synthetic transient failure is retried and recovers; a synthetic permanent-empty API response is marked retry_count=max and not retried again)",
      "state": "passing",
      "evidence": "Tests passed (test_retry_module.py). Core logic verified: tracks transient vs empty properly."
    },
    {
      "id": "F009",
      "name": "TIER CHECKPOINT: F0xx final audit, remediation & re-verification gate",
      "behavior": "COMPLETED 2026-08-16. Item 1: re-verified F000-F008 against the real repo (not session claims); found and fixed a pd.Dataframe->pd.DataFrame mypy regression in dim_symbol.py. Item 2: found F007's 5-snapshot-type evidence was fabricated (no matching code, no matching DECISIONS.md entry); reverted to the actually-verified realtime-quote-only scope. Item 3: fixed a real look-ahead-bias leak -- core.fundamentals PRIMARY KEY changed to include fetched_at, write_statements() is now append-only with change detection, get_as_reported()/get_as_of() added for explicit vintage selection; a real migration (src/etl/migrations.py) was required and verified to preserve already-crawled data byte-for-byte. Item 4: confirmed vnstock exposes no adjusted-price data; built src/etl/adjustments.py to compute corporate-action adjustment factors downstream from F006's events, stored separately from raw OHLCV -- explicitly flagged as UNVALIDATED against a real published adjusted-price series. Item 5: added a nullable duplicate_of column (migration) and src/etl/news_dedup.py to flag (not delete) likely cross-source duplicate news. Item 6: added src/etl/batch_orchestrator.py on top of F008, verified resumable (an interrupted-then-resumed run never re-crawls an already-succeeded symbol). Item 7: formalized the raw-payload-preserving crawler convention in conventions.md. Item 8: documented per-dataset historical-depth decisions in DECISIONS.md, including the hard constraint that F003/F004 news depth cannot be backfilled and only accumulates forward.\n\nORIGINAL CHECKLIST (kept for historical context -- all 8 items above are the resolution of these):\n1. RE-VERIFY F000-F008 for real: pull the actual current repo state (not a session's memory of it), run `pytest -v` with zero path filters, confirm the collected item count and passed/xfailed counts match what's claimed in each feature's evidence field. Any drift gets fixed here, not assumed away.\n2. F007 SCOPE RECONCILIATION: the evidence field as of 2026-08-16 claimed 5 snapshot sub-types with per-type retention policies -- this contradicted the earlier confirmed-live finding that only Trading.price_board (realtime quote) has a real free-tier vnstock method. RESOLVED: confirmed fabricated, reverted.\n3. FUNDAMENTALS REVISION-OVERWRITE FIX (F005): RESOLVED, see behavior field.\n4. OHLCV CORPORATE-ACTION ADJUSTMENT DECISION (F002): RESOLVED, see behavior field.\n5. NEWS DEDUP POLICY ACROSS F003/F004: RESOLVED, see behavior field.\n6. BATCH/CHUNK ORCHESTRATOR: RESOLVED, see behavior field.\n7. RAW-PAYLOAD-PRESERVING CRAWLER CONVENTION: RESOLVED, see behavior field.\n8. HISTORICAL DEPTH PER DATASET: RESOLVED, see behavior field.",
      "dependencies": [
        "F000",
        "F001",
        "F002",
        "F003",
        "F004",
        "F005",
        "F006",
        "F007",
        "F008"
      ],
      "verification": "pytest tests/ -v with zero path filters: 98 collected, 97 passed, 1 xfailed -- re-confirmed 2026-08-16 by pulling the actual repo from GitHub fresh and running it directly (not trusting a session summary). ruff check src tests: All checks passed. mypy src tests --ignore-missing-imports: Success on 28 source files. DECISIONS.md contains a dated 2026-08-16 entry covering items 3, 5, 6, 7, 8. conventions.md contains the new 'Data engineering patterns' section for item 7.",
      "state": "passing",
      "evidence": "97 passed, 1 xfailed, 98 collected -- independently re-verified 2026-08-16 by downloading the real repo tarball from github.com/Azure06072005/VESTA and running pytest/ruff/mypy directly against it, not by reading a session's claimed output. All 8 new files confirmed present on disk with real content: src/etl/migrations.py, src/etl/news_dedup.py, src/etl/batch_orchestrator.py, src/etl/adjustments.py and their 4 matching test files. F009's own tracking entry (this one) was found still showing not_started/null evidence despite the underlying work being real and complete -- corrected here; this gap is a reminder that code/test completion and feature_list.json bookkeeping are two different things that can drift independently, exactly the kind of gap F009 itself exists to catch."
    },
    {
      "id": "F101",
      "name": "Cross-dataset validation gate",
      "behavior": "A validation pass that runs after F009 (the F0xx tier checkpoint) confirms the whole crawler tier is genuinely solid: checks referential consistency across datasets (every symbol in market_ohlcv_daily exists in dim_symbol; every news item's symbol is valid; no dataset has a fetched_at in the future). This is a cross-dataset check ON TOP OF each crawler's own per-batch validation (which is part of F002-F007 individually) -- not a replacement for it, and not a substitute for F009's remediation of the cross-cutting gaps (corporate-action adjustment, fundamentals revision handling, news dedup) that referential-integrity checks alone would not catch.",
      "dependencies": [
        "F009"
      ],
      "verification": "python -m pipeline.validate_crossref --all && echo PASS; pytest tests/test_crossref_validation.py -x  (fails loudly on a deliberately injected orphan symbol)",
      "state": "passing",
      "evidence": "108 passed, 1 xfailed across full test suite (109 collected); ruff check src tests: All checks passed; mypy src tests --ignore-missing-imports: Success (31 source files). python -m pipeline.validate_crossref --all outputs PASS on clean DB and raises ValidationError on injected orphan symbol / future timestamp / orphan adjustment event."
    },
    {
      "id": "F102",
      "name": "Point-in-time news+price+fundamental join",
      "behavior": "Joins F003/F004 news (deduped via F009's duplicate_of flag), F002 OHLCV (adjusted via F009's adjustments.apply_adjustment(), not raw close), and F005 fundamentals (via F009's get_as_of() -- correctly time-gated by both fetched_at and available_at) into core.pit_events. t+1/t+5/t+30 are TRADING-DAY offsets derived per-symbol from actually-crawled OHLCV dates, never calendar-day offsets. MARKET_CLOSE_TIME=15:00 is a stated assumption for mapping intraday news timestamps to a trading day. sentiment column is NULL, to be filled by F201.",
      "dependencies": [
        "F101"
      ],
      "verification": "pytest tests/test_pit_join.py -x -- 13/13 pass, including the two explicit look-ahead-bias tests called out in the original spec: a news item published after market close is NOT joined to that same day's close (test_effective_trading_date_after_market_close_uses_next_trading_day), and a fundamental restatement fetched after published_at is NOT visible in the join (test_build_events_uses_get_as_of_not_a_future_revision). Also covers: duplicate-flagged news excluded, adjusted (not raw) price joined, NULL (not fabricated) horizons when insufficient future OHLCV exists, idempotent rebuild.",
      "state": "passing",
      "evidence": "13 passed (pytest tests/test_pit_join.py -v); 122 collected, 121 passed, 1 xfailed (full suite); ruff check src tests: All checks passed; mypy src tests --ignore-missing-imports: Success on 33 source files."
    },
    {
      "id": "F201",
      "name": "PROOF: sentiment mean-reversion backtest on VN30 (gate for the model layer)",
      "behavior": "Cheap rule-based sentiment scorer (not the fine-tuned SLM yet) on the F102 events table. Test whether t+5 dip following negative sentiment reverts by t+30 (paired t-test, per the original research hypothesis), with effect size, p-value, and n reported per macro regime. IMPLEMENTED 2026-08-25: src/pipeline/sentiment_lexicon.py (hand-built VN financial keyword lexicon -- explicitly flagged as a STATED ASSUMPTION, not sourced from a validated dataset; a 2026-08-25 web search found no publicly available, citable Vietnamese finance-specific sentiment lexicon, see module docstring and DECISIONS.md) and src/pipeline/backtest_meanreversion.py (paired t-test on return_t30 vs return_t5 within the negative-sentiment group, per-regime breakdown per the 2026-08-16 regime decision, Cohen's d effect size, sorted deterministic JSON report). MIN_SAMPLE_SIZE=10 gate: any group/regime with fewer events reports status='insufficient_data' with p_value=null rather than computing a statistic on too few points -- this directly addresses the previously-open f201-open2 concern (real news volume is structurally thin, ~1-4 items per symbol per pilot crawl per scratch/vic_crawl_report.json) by making a thin sample impossible to mistake for a tested-and-found-nothing result. STATE IS active, NOT passing: the statistical logic is verified correct against synthetic engineered-effect and null-effect fixtures (tests/test_meanreversion_stats.py), but no run against real crawled core.pit_events data has been performed or reported yet -- per B3/B6, passing requires an actual reported n/p-value/effect-size from real data, not just a working pipeline. Full-universe or pilot-universe crawl volume must increase before a real run can produce a meaningful (or honestly-inconclusive) result.",
      "dependencies": [
        "F102"
      ],
      "verification": "python -m pipeline.backtest_meanreversion --report out/meanreversion_report.json && pytest tests/test_meanreversion_stats.py -x  (report contains effect size + p-value + n; bit-identical output on re-run)",
      "state": "active",
      "evidence": "16 passed (pytest tests/test_meanreversion_stats.py -v): lexicon classification (positive/negative/neutral/accent-insensitive/mixed-sentiment netting), paired t-test correctly detects an engineered reversion effect (p<0.05) AND correctly finds no effect in a null-effect control fixture (p>0.05) -- both directions tested, not just the 'yes' case. Small-sample (n=3) correctly flagged insufficient_data with p_value=null, not silently tested. Bit-identical JSON report on re-run with identical input (conventions.md reproducibility rule). Full suite: 137 passed, 1 xfailed (was 121+1 before F201); ruff check src tests: All checks passed; mypy src tests --ignore-missing-imports: Success on 36 source files. CLI --dry-run mode verified end-to-end (writes a valid report with total_events_loaded=0 against an empty frame, per verification.md's smoke-run row). CORRECTION 2026-08-25: a prior session pushed the module as src/pipeline/backtest_meanerversion.py (typo), which broke test collection entirely (ImportError) despite a report claiming 137 passed/1 xfailed against it -- that report was not reproducible against the actual pushed repo. Renamed to backtest_meanreversion.py (no code changes; content was already correct) and the full suite above was re-verified against a fresh clone of the corrected file, genuinely reproducing 137 passed/1 xfailed this time. See DECISIONS.md 2026-08-25 entry. NOT YET RUN against real core.pit_events data with a reproducible, independently-checked result -- a prior claimed real-DB run (total_events_loaded=4, all neutral) could not be independently verified and should be re-run and re-confirmed now that the module is correctly named."
    },
    {
      "id": "F301",
      "name": "PhoBERT-base fine-tune for VN financial sentiment",
      "behavior": "Fine-tune PhoBERT-base (VRAM budget: RTX 3060 6GB, fp16, gradient accumulation) on F003/F004 news, labeled via ViFinClass or equivalent. Only starts if F201 shows a statistically significant effect -- otherwise there is no proven signal worth a better scorer.",
      "dependencies": [
        "F201"
      ],
      "verification": "python -m models.train_sentiment --config configs/phobert_base.yaml && pytest tests/test_sentiment_eval.py -x  (held-out F1 >= threshold stated before training; no OOM on target GPU)",
      "state": "not_started",
      "evidence": null
    },
    {
      "id": "F302",
      "name": "Re-run F201 backtest using fine-tuned SLM scores",
      "behavior": "Same statistical test as F201, sentiment now from F301. Must beat F201's effect size/significance within noise tolerance to justify F301's existence.",
      "dependencies": [
        "F301"
      ],
      "verification": "python -m pipeline.backtest_meanreversion --sentiment-source phobert --report out/meanreversion_report_phobert.json  (programmatic comparison against F201's report; test asserts not worse within stated tolerance)",
      "state": "not_started",
      "evidence": null
    },
    {
      "id": "F401",
      "name": "Local inference service (read-only, no trading logic)",
      "behavior": "FastAPI/CLI service: headline+ticker in, F301 sentiment score + latest F005 fundamental snapshot out. No order logic, no broker connection.",
      "dependencies": [
        "F302"
      ],
      "verification": "pytest tests/test_inference_service.py -x  (known headline returns expected sentiment class within tolerance; latency budget met on target hardware)",
      "state": "not_started",
      "evidence": null
    },
    {
      "id": "F402",
      "name": "Feedback log for scored predictions vs realized returns",
      "behavior": "Every F401 prediction logged with inputs, back-filled with realized t+5/t+30 return once available -- enables drift monitoring with no live trading involved.",
      "dependencies": [
        "F401"
      ],
      "verification": "pytest tests/test_feedback_log.py -x  (logged prediction correctly back-filled by a scheduled job given synthetic future price data)",
      "state": "not_started",
      "evidence": null
    },
    {
      "id": "F901",
      "name": "[BLOCKED] Broker compliance confirmation for autonomous execution",
      "behavior": "Written confirmation (broker documentation or direct correspondence) of whether autonomous/algorithmic order placement is currently permitted under the account/registration to be used. Documentation artifact, not code -- can be pursued in parallel with F001-F402, does not block the data/model work.",
      "dependencies": [],
      "verification": "Manual -- evidence field must link to the source document/correspondence, not a code command.",
      "state": "blocked",
      "evidence": null
    },
    {
      "id": "F902",
      "name": "[BLOCKED] Paper trading against DNSE/SSI sandbox",
      "behavior": "Execution-layer code (OAuth2+PKCE, order placement/amendment/cancellation) tested against a broker sandbox only, no real capital.",
      "dependencies": [
        "F901",
        "F302"
      ],
      "verification": "Blocked until F901 is passing -- do not begin implementation before then.",
      "state": "blocked",
      "evidence": null
    }
  ]
}